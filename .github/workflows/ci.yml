name: CI

on:
  push:
    branches: [main]
  pull_request:

# ═══════════════════════════════════════════════════════════════════════════════
# Portões de CI — doc 00, seção 11.
#
# Seis portões. Os três primeiros são higiene; os três últimos existem porque a
# violação correspondente é uma falha de segurança ou de governança de dados que
# revisão humana não pega de forma confiável.
# ═══════════════════════════════════════════════════════════════════════════════

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  qualidade:
    name: Lint, tipos e build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Sem `version:` — a fonte é o campo packageManager do package.json.
      # Declarar nos dois lugares faz a action abortar com ERR_PNPM_BAD_PM_VERSION.
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile

      # Portão 1 — lint. Inclui as regras de FRONTEIRA de import (eslint.config.mjs):
      # o gateway externo não alcança módulo interno; o dicionário de métricas não
      # depende de aplicação; as superfícies não reusam tela entre si.
      - name: Portão 1 — lint e fronteiras de import
        run: pnpm lint

      - name: Portão 2 — tipos
        run: pnpm typecheck

      - name: Portão 3 — build
        run: pnpm build

  isolamento:
    name: Isolamento de tenant
    runs-on: ubuntu-latest

    # ─────────────────────────────────────────────────────────────────────────
    # Portão 4 — O PORTÃO QUE MAIS IMPORTA.
    #
    # Roda contra Postgres real, não contra simulação. Verifica que o BANCO
    # recusa, mesmo que a aplicação erre: RLS forçado, tenant transaction-local
    # que não vaza no pool, papel externo com USAGE em exatamente um esquema,
    # k-anonimato imposto por CHECK, `fact` append-only.
    #
    # `--test-force-exit` não é usado de propósito: se o teste for pulado por
    # falta de banco, o passo seguinte falha explicitamente. Suíte de segurança
    # que passa por não ter rodado é pior que suíte que falha.
    # ─────────────────────────────────────────────────────────────────────────
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: teste
          POSTGRES_DB: pulse
        ports: ['5434:5432']
        options: >-
          --health-cmd "pg_isready -U postgres -d pulse"
          --health-interval 10s --health-timeout 5s --health-retries 10

    steps:
      - uses: actions/checkout@v4
      # Sem `version:` — a fonte é o campo packageManager do package.json.
      # Declarar nos dois lugares faz a action abortar com ERR_PNPM_BAD_PM_VERSION.
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @pulse/db build

      - name: Portão 4 — isolamento de tenant (Postgres real)
        env:
          DATABASE_URL_ADMIN: postgres://postgres:teste@127.0.0.1:5434/pulse
        run: |
          node --test packages/db/dist/rls.test.js 2>&1 | tee resultado.txt
          if grep -qE '^# skipped [1-9]' resultado.txt; then
            echo "::error::Testes de isolamento foram PULADOS. Sem banco, este portão não protege nada."
            exit 1
          fi

      # Invariantes de domínio: as regras que o banco impõe porque disciplina de
      # processo não sobrevive a pressa — dedup da fila, as duas confirmações do
      # efeito na receita, cascata que fecha, competência congelada.
      - name: Portão 4b — invariantes de domínio (Postgres real)
        env:
          DATABASE_URL_ADMIN: postgres://postgres:teste@127.0.0.1:5434/pulse
        run: |
          node --test packages/db/dist/invariantes.test.js 2>&1 | tee inv.txt
          if grep -qE '^# skipped [1-9]' inv.txt; then
            echo "::error::Invariantes de domínio foram PULADAS."
            exit 1
          fi

      # O executor de ciclos: a semântica de falha só é verificável contra banco
      # real, porque o que importa é o efeito em watermark, cycle_run e trava.
      - name: Portão 4c — executor de ciclos (Postgres real)
        env:
          DATABASE_URL_ADMIN: postgres://postgres:teste@127.0.0.1:5434/pulse
        run: |
          pnpm --filter @pulse/worker build
          node --test apps/worker/dist/runner.test.js 2>&1 | tee run.txt
          if grep -qE '^# skipped [1-9]' run.txt; then
            echo "::error::Testes do executor foram PULADOS."
            exit 1
          fi

      # A massa sintética é o que destrava construir o produto sem a réplica.
      # Reexecução é a operação mais comum, e é justamente a que falha primeiro.
      - name: Portão 4d — seed contra banco real
        env:
          DATABASE_URL_ADMIN: postgres://postgres:teste@127.0.0.1:5434/pulse
        run: |
          node --test packages/db/dist/seed/seed-db.test.js 2>&1 | tee seed.txt
          if grep -qE '^# skipped [1-9]' seed.txt; then
            echo "::error::Testes do seed foram PULADOS."
            exit 1
          fi

      # A consolidação: primeiro caminho de ponta a ponta do repositório —
      # fatos gerados, sinais calculados, agregados publicados com supressão.
      - name: Portão 4e — consolidação (Postgres real)
        env:
          DATABASE_URL_ADMIN: postgres://postgres:teste@127.0.0.1:5434/pulse
        run: |
          node --test apps/worker/dist/consolidacao.test.js 2>&1 | tee cons.txt
          if grep -qE '^# skipped [1-9]' cons.txt; then
            echo "::error::Testes da consolidação foram PULADOS."
            exit 1
          fi

      # A fila é o que o CSM abre de manhã. As quatro regras que decidem se ela
      # continua sendo usada no terceiro mês — teto, deduplicação por família,
      # carência e modo sombra — só se testam contra banco real.
      - name: Portão 4f — fila de trabalho (Postgres real)
        env:
          DATABASE_URL_ADMIN: postgres://postgres:teste@127.0.0.1:5434/pulse
        run: |
          # Os dois geradores de item no mesmo portão, e em série: o teste do teto
          # do gerador contratual conta a fila que o outro deixou.
          node --test --test-concurrency=1 \
            apps/worker/dist/fila.test.js \
            apps/worker/dist/contratual.test.js 2>&1 | tee fila.txt
          if grep -qE '^# skipped [1-9]' fila.txt; then
            echo "::error::Testes da fila foram PULADOS."
            exit 1
          fi

      # O recorte das telas e o fluxo de saída: quem vê qual item, quem abre
      # qual conta, e as duas confirmações sem as quais a receita não se move. As
      # asserções são sobre o que NÃO aparece — a classe de erro que passa numa
      # revisão de tela, porque a tela fica bonita e mostra demais.
      - name: Portão 4i — recorte de carteira na ESCRITA (Postgres real)
        env:
          DATABASE_URL_ADMIN: postgres://postgres:teste@127.0.0.1:5434/pulse
        run: |
          pnpm --filter @pulse/success build
          node --test --test-concurrency=1 packages/success/dist/recorte.test.js

      - name: Portão 4h — recorte da fila e do Cliente 360 (Postgres real)
        env:
          DATABASE_URL_ADMIN: postgres://postgres:teste@127.0.0.1:5434/pulse
        run: |
          # --test-concurrency=1 é obrigatório: os dois arquivos truncam as
          # MESMAS tabelas, e em paralelo um limpa o cenário do outro. A falha
          # seria intermitente — a pior classe, porque some quando se investiga.
          pnpm --filter @pulse/contratos build
          node --test --test-concurrency=1 \
            packages/success/dist/fila.test.js \
            packages/success/dist/conta.test.js \
            packages/success/dist/cancelamento.test.js \
            packages/success/dist/fechamento.test.js \
            packages/success/dist/biblioteca.test.js \
            packages/success/dist/renovacao.test.js \
            packages/contratos/dist/clausula.test.js \
            packages/contratos/dist/calendario.test.js \
            packages/success/dist/carteira.test.js \
            packages/success/dist/relatorio.test.js 2>&1 | tee sup.txt
          if grep -qE '^# skipped [1-9]' sup.txt; then
            echo "::error::Testes de recorte da superfície foram PULADOS."
            exit 1
          fi

      # A declaração dos ciclos alimenta o painel de pipeline: ele precisa
      # mostrar o que está de fato rodando, não o que estava no código dele.
      - name: Portão 4g — declaração dos ciclos (Postgres real)
        env:
          DATABASE_URL_ADMIN: postgres://postgres:teste@127.0.0.1:5434/pulse
        run: node --test apps/worker/dist/registro.test.js

      # Portão 5 — migrations aplicam E revertem em banco limpo.
      # Migration que sobe e não desce transforma rollback em incidente.
      - name: Portão 5 — migrations idempotentes
        env:
          DATABASE_URL_ADMIN: postgres://postgres:teste@127.0.0.1:5434/pulse
        run: |
          # Reaplicar não deve mudar nada nem falhar.
          node packages/db/dist/migrate.js
          node packages/db/dist/migrate.js

  governanca:
    name: Governança de métricas
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Sem `version:` — a fonte é o campo packageManager do package.json.
      # Declarar nos dois lugares faz a action abortar com ERR_PNPM_BAD_PM_VERSION.
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile

      # Portão 6 — toda métrica tem dono, fórmula e explicação; nenhuma está
      # duplicada; os pesos dos drivers somam 100. Doc 00, 6.5.
      # Sem isto, "dicionário de definições com responsável nomeado" volta a ser
      # uma promessa de documento.
      - name: Portão 6 — dicionário de métricas
        run: |
          pnpm --filter @pulse/metrics build
          node --test packages/metrics/dist/metrics.test.js
          node --test packages/metrics/dist/drivers.test.js
          # Verifica os ARQUIVOS de migration, sem banco — mas precisa do dist.
          pnpm --filter @pulse/db build
          node --test packages/db/dist/migracoes.test.js
          # A lista de papéis vive no TypeScript E num CHECK do banco. Este portão
          # recusa a divergência — que já custou uma gravação quebrada.
          node --test packages/db/dist/papeis.test.js
          node --test packages/metrics/dist/gatilhos.test.js
          pnpm --filter @pulse/success build && node --test packages/success/dist/calibracao.test.js
          pnpm --filter @pulse/config build
          node --test --test-concurrency=1 packages/config/dist/sincronizar-core.test.js
          node --test packages/success/dist/idor.test.js
          pnpm --filter @pulse/config build
          node --test packages/config/dist/catalogo.test.js
          node --test packages/config/dist/hubspot-vinculo.test.js
          node --test packages/config/dist/base-de-clientes.test.js
          node --test packages/config/dist/uso.test.js
          # Credencial de cliente na tela de conferência: a aba "todos os campos"
          # despejava api_secret/api_key/signature_secret da Lecupon em página interna.
          node --test packages/config/dist/fontes.test.js
          node --test packages/auth/dist/cifra.test.js

      - name: Portão 8 — segredos sem placeholder
        # Não decifra (o CI não tem a chave age): confere que o ARQUIVO cifrado não
        # tem chave faltando e que o script do portão continua executável. A
        # verificação de conteúdo roda na VM, em `make secrets-decrypt`.
        run: |
          test -f infra/secrets/pulse.env.sops.yaml
          bash -n infra/secrets/verificar.sh
          for k in PULSE_PROXY_SECRET PULSE_CHAVE_MESTRA PULSE_API_PASSWORD \
                   PULSE_WORKER_PASSWORD POSTGRES_PULSE_PASSWORD; do
            grep -q "^$k:" infra/secrets/pulse.env.sops.yaml || { echo "falta $k no SOPS"; exit 1; }
          done
          # E que nada foi commitado em texto claro por engano.
          ! grep -qE '^[A-Z_]+: (trocar|change|placeholder)$' infra/secrets/pulse.env.sops.yaml

      - name: Portão 7 — design system (sem banco, sem build)
        run: node --test packages/ui/design-system.test.mjs

      - name: Portão 8 — todo teste é rodado por este arquivo
        # Recusa teste que existe em disco e que nenhum job nomeia. A lista
        # abaixo é escrita à mão, e lista duplicada diverge — mesma razão de
        # papeis.test.ts e migracoes.test.ts existirem.
        run: node --test packages/ui/testes-no-ci.test.mjs

      - name: Rota de retorno da tela de login (sem build)
        # Fica em @pulse/ui, que não emite dist; roda por remoção de tipos.
        run: node --test --experimental-strip-types packages/ui/src/rotaInterna.test.ts

      - name: Testes das demais unidades
        run: |
          pnpm --filter @pulse/auth build
          node --test packages/auth/dist/auth.test.js
          node --test packages/auth/dist/cifra.test.js
          node --test packages/auth/dist/verificacao.test.js
          pnpm --filter @pulse/mail build && node --test packages/mail/dist/mailer.test.js
          pnpm --filter @pulse/config build && node --test packages/config/dist/core-lecupon.test.js
          node --test packages/config/dist/sincronizacao.test.js
          pnpm --filter @pulse/contracts build && node --test packages/contracts/dist/contracts.test.js
          pnpm --filter @pulse/worker build
          node --test apps/worker/dist/cycle.test.js
          node --test apps/worker/dist/quality.test.js
          node --test apps/worker/dist/alarme.test.js
          pnpm --filter @pulse/db build
          node --test packages/db/dist/seed/seed.test.js
